make.jointRV <- function(..., probs){
    # Create a list from the inputs
    # and name them accordingly
    # so that we can do stuff like this:
    # myjoint <- make.jointRV(X = c(0,0,1,1), 
    #                         Y = c(0,1,0,1), 
    #                         probs = c(.1,.5,.2,.2))
    # with(myjoint, P(X > Y))
    .NotYetImplemented()
}